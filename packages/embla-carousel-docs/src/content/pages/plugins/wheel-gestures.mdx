---
title: Wheel Gestures
description: Learn how to add the Wheel Gesture plugin to enable scrolling the carousel with mouse wheel or touchpad gestures in Embla Carousel.
order: 70
date: 2025-11-02
---

import { Tabs } from 'components/Tabs/Tabs'
import { TabsItem } from 'components/Tabs/TabsItem'
import { TABS_PACKAGE_MANAGER } from 'consts/tabs'
import { PrismHighlight } from 'components/Mdx/Components/PrismHighlight'
import wheelGesturesInstallation_cdn_01 from '!!raw-loader!content/code-snippets/plugins/wheel-gestures/installation-cdn-01.html'
import wheelGesturesInstallation_npm_01 from '!!raw-loader!content/code-snippets/plugins/wheel-gestures/installation-npm-01.sh'
import wheelGesturesInstallation_yarn_01 from '!!raw-loader!content/code-snippets/plugins/wheel-gestures/installation-yarn-01.sh'

# Wheel Gestures

<RepositoryLink to="https://github.com/xiel/embla-carousel-wheel-gestures">
  View plugin on GitHub
</RepositoryLink>

This plugin adds **mouse and trackpad wheel support** to Embla Carousel, allowing users to navigate slides using scroll gestures.

---

## Installation

First you need to install the **npm package** and save it to your dependencies:

<Tabs groupId={TABS_PACKAGE_MANAGER.GROUP_ID}>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.CDN}>
    <PrismHighlight language="html" code={wheelGesturesInstallation_cdn_01} />
  </TabsItem>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.NPM}>
    <PrismHighlight language="shell" code={wheelGesturesInstallation_npm_01} />
  </TabsItem>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.YARN}>
    <PrismHighlight language="shell" code={wheelGesturesInstallation_yarn_01} />
  </TabsItem>
</Tabs>

## Options

Below follows an exhaustive **list of all** `Wheel Gestures` **options** and their default values.

---

### wheelDraggingClass

<ApiMetaData
  firstRow={['Type', 'string']}
  secondRow={['Default', 'is-wheel-dragging']}
/>
<PrismHighlight
  language="ts"
  hideLabel
  code={`{ wheelDraggingClass: 'is-wheel-dragging' }`}
/>

Choose a classname that will be applied to the container during a wheel gesture. Pass an empty string to opt-out.

---

### forceWheelAxis

<ApiMetaData
  firstRow={['Type', 'string']}
  secondRow={['Default', 'undefined']}
/>
<PrismHighlight
  language="ts"
  hideLabel
  code={`{ forceWheelAxis: 'x' | 'y' }`}
/>

Force an axis on which to listen for wheel events. Choose scroll axis between `x` and `y`. Useful if you want to slide horizontally when scrolling vertically or vice versa.

---

### target

<ApiMetaData
  firstRow={['Type', 'Element']}
  secondRow={['Default', 'undefined']}
/>
<PrismHighlight
  language="ts"
  hideLabel
  code={`{ target: document.querySelector('.embla__viewport') }`}
/>

Specify the element that should be observed for wheel events.

---
