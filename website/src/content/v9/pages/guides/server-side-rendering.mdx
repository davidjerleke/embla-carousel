export const metadata = {
  title: 'Server-Side Rendering',
  description: `This guide explains how to configure Embla Carousel for server-side rendering (SSR) environments.`,
  order: 90
}

import { TABS_LIBRARY, TABS_PACKAGE_MANAGER } from '@/utils/tabs'

import ssrInstallation_cdn_01 from '@/content/v9/code-snippets/plugins/ssr/installation-cdn-01.html'
import ssrInstallation_npm_01 from '@/content/v9/code-snippets/plugins/ssr/installation-npm-01.sh'
import ssrInstallation_yarn_01 from '@/content/v9/code-snippets/plugins/ssr/installation-yarn-01.sh'
import ssrInstallation_pnpm_01 from '@/content/v9/code-snippets/plugins/ssr/installation-pnpm-01.sh'

import settingUpSsr_css_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-css-01.less'
import settingUpSsr_module_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-vanilla-01.js'
import settingUpSsr_module_02 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-vanilla-02.js'
import settingUpSsr_react_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-react-01.jsx'
import settingUpSsr_vue_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-vue-01.vue'
import settingUpSsr_solid_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-solid-01.jsx'
import settingUpSsr_svelte_01 from '@/content/v9/code-snippets/guides/server-side-rendering/setting-up-ssr-svelte-01.svelte'
import breakpoints_css_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-css-01.less'
import breakpoints_module_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-vanilla-01.js'
import breakpoints_module_02 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-vanilla-02.js'
import breakpoints_react_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-react-01.jsx'
import breakpoints_vue_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-vue-01.vue'
import breakpoints_solid_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-solid-01.jsx'
import breakpoints_svelte_01 from '@/content/v9/code-snippets/guides/server-side-rendering/breakpoints-svelte-01.svelte'

# Server-Side Rendering

This guide explains how to configure Embla Carousel for **server-side rendering (SSR)** environments and when to use SSR-specific settings.

---

## Prerequisites

Before continuing with this guide, make sure you've completed the following:

- [How Embla Carousel Works](/docs/guides/how-embla-carousel-works)
- [Required Setup](/docs/guides/required-setup)
- [Slide Sizes](/docs/guides/slide-sizes)
- [Slide Gaps](/docs/guides/slide-gaps)

## Understanding SSR

Embla Carousel is **environment-agnostic**, meaning it works in both client-side and server-side contexts. In many cases, you can render the carousel markup on the server **without any SSR-specific configuration**, and everything will work as expected.

However, there are scenarios where you may want to **prevent layout shifts** caused by Embla's initialization when the browser dimensions become available. In such cases, Embla provides optional SSR tools so the carousel renders with stable positioning immediately.

You should enable SSR support if **any** of these are true:

- [`loop`](/docs/api/options#loop) is `true`.
- [`containScroll`](/docs/api/options#containscroll) is `false`.
- [`startSnap`](/docs/api/options/#startsnap) is set to anything other than `0`.

## Setting Up SSR

Start by installing the **SSR plugin** package:

<Tabs groupId={TABS_PACKAGE_MANAGER.GROUP_ID}>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.CDN}>
    <PrismHighlight language="html" code={ssrInstallation_cdn_01} />
  </TabsItem>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.NPM}>
    <PrismHighlight language="shell" code={ssrInstallation_npm_01} />
  </TabsItem>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.PNPM}>
    <PrismHighlight language="shell" code={ssrInstallation_pnpm_01} />
  </TabsItem>
  <TabsItem tab={TABS_PACKAGE_MANAGER.TABS.YARN}>
    <PrismHighlight language="shell" code={ssrInstallation_yarn_01} />
  </TabsItem>
</Tabs>

Next, ensure your carousel's CSS uses **percentage-based slide widths**. Here's an example where each slide takes up **50% of the viewport width**:

<PrismHighlight language="css" code={settingUpSsr_css_01} highlight="11" />

Then, enable **SSR support** by adding the [`Ssr`](/docs/plugins/ssr) plugin and generating server-side styles using the [`getStyles`](/docs/plugins/ssr/#getstyles) method.

You'll specify slide widths as **percentages (%)**, exactly matching your CSS:

<Tabs groupId={TABS_LIBRARY.GROUP_ID}>
  <TabsItem tab={TABS_LIBRARY.TABS.VANILLA}>
    <PrismHighlight
      language="js"
      code={settingUpSsr_module_01}
      highlight="3,6-10,14"
    />
    <PrismHighlight language="js" code={settingUpSsr_module_02} />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.REACT}>
    <PrismHighlight
      language="jsx"
      code={settingUpSsr_react_01}
      highlight="3,7-10,17"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.VUE}>
    <PrismHighlight
      language="html"
      asLanguage="vue"
      code={settingUpSsr_vue_01}
      highlight="4,14-17,22"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.SOLID}>
    <PrismHighlight
      language="jsx"
      code={settingUpSsr_solid_01}
      highlight="3,6-9,17"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.SVELTE}>
    <PrismHighlight
      language="svelte"
      code={settingUpSsr_svelte_01}
      highlight="3,9,12,17"
    />
  </TabsItem>
</Tabs>

<Admonition type="warning">
  **Warning:** Your CSS widths (such as `flex: 0 0 50%`) must exactly match the
  values in the [`slideSizes`](/docs/plugins/ssr/#slidesizes) array. Any
  mismatch will result in incorrect snapping and layout shifts during hydration.
</Admonition>

Since the server has **no access to runtime measurements**, you must explicitly define the slide sizes. This introduces a few key requirements:

- Slides must use **percentage-based widths**.
- Slide spacing must use the [padding method](/docs/guides/slide-gaps) (`margin` and `gap` are not supported during SSR).
- CSS widths must **exactly match** the values in [`slideSizes`](/docs/plugins/ssr/#slidesizes).
- Each carousel must have a **unique selector** to avoid SSR styles affecting others on the page.

<Admonition type="note">
  **Note:** SSR also fully supports **vertical carousels**, as long as the CSS
  is configured so that slides stack vertically (e.g. `flex-direction: column`)
  and the percentage sizes apply to height instead of width. The same SSR rules
  apply — the CSS values must match the values provided in the
  [`slideSizes`](/docs/plugins/ssr/#slidesizes) array.
</Admonition>

## Breakpoints

Embla also supports SSR configurations that change with **CSS breakpoints**. Start by defining your CSS with different slide widths per viewport size:

<PrismHighlight language="css" code={breakpoints_css_01} highlight="15-19" />

Then provide matching [`slideSizes`](/docs/plugins/ssr/#slidesizes) values for each breakpoint using the [`breakpoints`](/docs/plugins/ssr/#breakpoints) option. You can define unique slide sizes and settings for each breakpoint, and Embla will apply them correctly on the server.

<Tabs groupId={TABS_LIBRARY.GROUP_ID}>
  <TabsItem tab={TABS_LIBRARY.TABS.VANILLA}>
    <PrismHighlight
      language="js"
      code={breakpoints_module_01}
      highlight="9-16"
    />
    <PrismHighlight language="js" code={breakpoints_module_02} />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.REACT}>
    <PrismHighlight
      language="jsx"
      code={breakpoints_react_01}
      highlight="9-16"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.VUE}>
    <PrismHighlight
      language="html"
      asLanguage="vue"
      code={breakpoints_vue_01}
      highlight="16-23"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.SOLID}>
    <PrismHighlight
      language="jsx"
      code={breakpoints_solid_01}
      highlight="8-15"
    />
  </TabsItem>
  <TabsItem tab={TABS_LIBRARY.TABS.SVELTE}>
    <PrismHighlight
      language="svelte"
      code={breakpoints_svelte_01}
      highlight="9-16"
    />
  </TabsItem>
</Tabs>

---
