import { css } from 'styled-components'

/* CONSTS */
export const TRUNCATE_STYLES = css`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`

export const TRUNCATE_FLEX_STYLES = css`
  min-width: 0;
`
